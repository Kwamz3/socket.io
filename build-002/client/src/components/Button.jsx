

export default function Button({props}) {
  return (
    <div>
          <button
              className= 'send-scores'
              props= {props}
              >{props}</button>
    </div>
  )
}
